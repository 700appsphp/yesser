<%- model_class = Organization -%>

<div class="box box-danger">
  <div class="box-header">
    <h3 class="box-title"><%=t '.title', :default => [:'helpers.titles.edit', 'Edit %{name} %{model}'],
                                  name: @organization.name, 
                                  model: model_class.model_name.human.titleize %></h3>
  </div>
  <%= render :partial => 'form' %>
</div>
